{
  "name": "vtexec",
  "version": "1.0.0",
  "description": "Start command in new terminal.",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src/ --ext ts --exec 'tsc && node dist/index.js'",
    "start": "node dist/index.js"
  },
  "keywords": [
    "open terminal",
    "terminal",
    "virtual terminal",
    "terminal emulator"
  ],
  "author": "Ando",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.27",
    "nodemon": "^2.0.4"
  }
}
